#pragma once

//

// Sample n elements from uniform distribution
// Normalized distribution of elements,
// int no. elements.
//
int sample_n_elements(int *normalized_distribution, int no_elements, int *dest, int target_elements);
  
